spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    com.kitabqurdu: INFO
    org.springframework.security: INFO
